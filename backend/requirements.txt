fastapi>=0.115
uvicorn[standard]>=0.32
asyncmy>=0.2.9          # OceanBase / MySQL-wire driver (NOT asyncpg/Postgres)
httpx>=0.27             # SalesCRM REST client
rapidfuzz>=3.9           # fuzzy name matching (C++-backed, fast enough for 100k+ names)
openpyxl>=3.1            # Excel (.xlsx) upload parsing and results export
python-multipart>=0.0.9  # required by FastAPI for multipart/form-data file uploads
